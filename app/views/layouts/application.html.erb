<!DOCTYPE html>
<html>
  <head>
    <title>AFTERNOONTEADAYS</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- CSS -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- JS (Importmap 使用) -->
    <%= javascript_importmap_tags %>

    <!-- Favicon -->
    <%= favicon_link_tag 'favicon.ico' %>
  </head>

  <body>
    <% if user_signed_in? %>
      <header class="d-flex align-items-center justify-content-start bg-dark p-2">
        <!-- ロゴ＋サイト名 -->
        <div class="d-flex align-items-center mr-4">
          <%= link_to root_path, class: "d-flex align-items-center text-white nav-link" do %>
            <%= image_tag "favicon.png", alt: "AFTERNOONTEADAYSロゴ", class: "logo mr-2" %>
            <span>AFTERNOONTEADAYS</span>
          <% end %>
        </div>

        <!-- ナビゲーションリンク -->
        <div><%= link_to "トップ", root_path, class: "text-white nav-link" %></div>
        <div><%= link_to "投稿する", new_post_path, class: "text-white nav-link" %></div>
        <div><%= link_to "投稿一覧", posts_path, class: "text-white nav-link" %></div>
        <div><%= link_to "下書き一覧", confirm_posts_path, class: "text-white nav-link" %></div>
        <div><%= link_to "ユーザ一覧", users_path, class: "text-white nav-link" %></div>
        <div><%= link_to "マイページ", user_path(current_user.id), class: "text-white nav-link" %></div>

        <!-- ✅ Turboを無効にして、確実にログアウト -->
        <div>
          <%= button_to "ログアウト", destroy_user_session_path,
                method: :delete,
                data: { turbo: false },
                class: "btn btn-link text-white nav-link p-0" %>
        </div>
      </header>
    <% end %>

    <%= yield %>
  </body>
</html>
